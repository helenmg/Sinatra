source 'https://rubygems.org'
gem 'sinatra', '2.0.0'
gem 'datamapper'
gem 'shotgun'

group :development do
    gem 'sqlite3'
    gem 'dm-sqlite-adapter'
  end
   
  group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
  end
